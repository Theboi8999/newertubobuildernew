import type { RbxPart } from '../rbxmx'

// TODO: Replace this stub with the 185 parts parsed from Building.rbxmx.
// Each part should be a literal RbxPart object with exact position, size,
// color, material, and r00-r22 rotation matrix values from the source file.
export function buildOttomanHouse(_wallBase: number): RbxPart[] {
  return []
}
